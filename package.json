{
  "name": "timestamp-microservice-project",
  "version": "1.0.0",
  "description": "Displays the unix timestamp and utc date of a date given in either format",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server",
    "build": "cd client/ && npm install && npm run build"
  },
  "keywords": [],
  "author": "Bryan O",
  "license": "ISC",
  "engines": {
    "node": "14.16.0",
    "npm": "7.8.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
